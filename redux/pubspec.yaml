name: flutterritory
description: Flutterritory - Redux

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  repository:
    path: ../repository
  cupertino_icons: ^0.1.2
  flutter:
    sdk: flutter
  flutter_redux: ^0.5.3
  redux_logging: ^0.3.0
  url_launcher: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true